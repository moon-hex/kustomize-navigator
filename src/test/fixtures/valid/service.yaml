apiVersion: v1
kind: Service
metadata:
  name: test-service
  labels:
    app: test-app
spec:
  selector:
    app: test-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: v1
kind: namespace
metadata:
  name: test-namespace
  labels:
    app: test-app
    environment: test
